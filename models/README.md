# Models

Trained Faster R-CNN checkpoints are not stored in git (they exceed GitHub file limits).

Expected path after training:

```
models/detector/faster_rcnn_multiclass_v2_best.pth
```

Train locally:

```bash
python src/detector/train_detector.py
```
